# StegArmor v1.0 
An advanced cross-platform CLI tool combining AES-256 bit encryption and Least Significant Bit (LSB) Steganography to hide confidential data invisibly inside loss-less PNG images.

##  Quick Setup
```bash
pip install -r requirements.txt
